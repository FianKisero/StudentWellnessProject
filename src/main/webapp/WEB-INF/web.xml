<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

  <display-name>Student Wellness Hub</display-name>

  <!-- ✅ Welcome Page -->
  <welcome-file-list>
    <welcome-file>login</welcome-file>
  </welcome-file-list>

  <!-- ✅ LOGIN SERVLET WITH INIT PARAMS -->
  <servlet>
    <servlet-name>login</servlet-name>
    <servlet-class>app.LoginPage</servlet-class>

    <init-param>
      <param-name>adminUser</param-name>
      <param-value>admin</param-value>
    </init-param>

    <init-param>
      <param-name>adminPass</param-name>
      <param-value>1234</param-value>
    </init-param>
  </servlet>

  <servlet-mapping>
    <servlet-name>login</servlet-name>
    <url-pattern>/login</url-pattern>
  </servlet-mapping>

  <!-- ✅ REGISTER -->
  <servlet>
    <servlet-name>register</servlet-name>
    <servlet-class>app.RegisterPage</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>register</servlet-name>
    <url-pattern>/register</url-pattern>
  </servlet-mapping>

  <!-- ✅ HOME -->
  <servlet>
    <servlet-name>home</servlet-name>
    <servlet-class>app.HomePage</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>home</servlet-name>
    <url-pattern>/home</url-pattern>
  </servlet-mapping>

  <!-- ✅ MOOD -->
  <servlet>
    <servlet-name>mood</servlet-name>
    <servlet-class>app.MoodPage</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>mood</servlet-name>
    <url-pattern>/mood</url-pattern>
  </servlet-mapping>

  <!-- ✅ TIPS -->
  <servlet>
    <servlet-name>tips</servlet-name>
    <servlet-class>app.TipsPage</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>tips</servlet-name>
    <url-pattern>/tips</url-pattern>
  </servlet-mapping>

  <!-- 🔥 IMPORTANT: LOGIN FILTER -->
  <filter>
    <filter-name>LoginFilter</filter-name>
    <filter-class>app.LoginFilter</filter-class>
  </filter>

  <filter-mapping>
    <filter-name>LoginFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <listener>
    <listener-class>app.SessionCounterListener</listener-class>
  </listener>

  <listener>
    <listener-class>app.AppContextListener</listener-class>
  </listener>
  <listener>
    <listener-class>app.AppContextListener</listener-class>
  </listener>
  <listener>
    <listener-class>app.SessionCounterListener</listener-class>
  </listener>
  <listener>
    <listener-class>app.MyRequestListener</listener-class>
  </listener>

</web-app>